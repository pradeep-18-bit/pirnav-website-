# Leave blank in production/Docker to use same-origin /api calls via nginx proxy.
VITE_API_BASE_URL=
# Local Vite dev server proxy target.
VITE_API_PROXY_TARGET=http://localhost:5001
VITE_DEFAULT_INTERVIEW_MANAGER_ID=1
